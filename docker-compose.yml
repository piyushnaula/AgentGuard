services:
  agentguard:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////app/data/agentguard.db
    volumes:
      - agentguard_data:/app/data

volumes:
  agentguard_data:
